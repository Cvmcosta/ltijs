dist
coverage
node_modules
*.md
*.html

# The docs website is an independently-tooled docsify project (its own package.json, loaded via CDN
# scripts in index.html) -- not formatted by the library's own prettier config.
website
